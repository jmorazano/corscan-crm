# ============================================================
# Vocero CRM — variables de entorno
# Copia este archivo a `.env` y reemplaza cada REEMPLAZA_...
# En producción estas variables van en tu plataforma de hosting
# (Coolify / docker compose), no solo en el archivo.
# ============================================================

# --- Aplicación ---------------------------------------------
# URL pública de la instancia (con https en producción).
# De ella se construye la URL del webhook de WhatsApp.
APP_BASE_URL=REEMPLAZA_https://crm.tudominio.com

# Solo Ruta B (docker compose): dominio para el HTTPS automático de Caddy.
DOMAIN=REEMPLAZA_crm.tudominio.com

# --- Base de datos ------------------------------------------
# Ruta B usa el servicio interno `postgres` del compose.
# Genera la contraseña con:  openssl rand -hex 24
POSTGRES_PASSWORD=REEMPLAZA_password_de_postgres
DATABASE_URL=postgresql://postgres:REEMPLAZA_password_de_postgres@postgres:5432/vocero

# --- Secretos de la aplicación ------------------------------
# Firma de sesiones. Genera con:  openssl rand -base64 32
BETTER_AUTH_SECRET=REEMPLAZA_secreto_de_sesiones

# Cifrado en reposo del token de WhatsApp (AES-256-GCM).
# DEBE ser exactamente 32 bytes en base64 (44 caracteres).
# Genera con:  openssl rand -base64 32
ENCRYPTION_KEY=REEMPLAZA_clave_de_cifrado_base64

# --- WhatsApp (webhook) -------------------------------------
# Token de verificación del webhook. Es también el segmento secreto
# de la URL del webhook: /api/webhooks/wa/<este_valor>
# Genera con:  openssl rand -hex 32
META_WEBHOOK_VERIFY_TOKEN=REEMPLAZA_token_de_verificacion

# OPCIONAL (recomendado en modo directo): App Secret de tu app de Meta
# para validar la firma de cada webhook (x-hub-signature-256).
# Se obtiene en developers.facebook.com → tu app → Configuración → Básica.
# En modo agencia normalmente se deja vacío (la URL secreta protege el webhook).
# META_APP_SECRET=

# Versión de la Graph API (déjala así salvo que sepas por qué cambiarla).
META_GRAPH_API_VERSION=v25.0

# --- Embedded Signup (opcional) -----------------------------
# Con estas tres variables, Ajustes → WhatsApp muestra el botón "Conectar con
# Meta": el negocio autoriza en un popup y no hay que pegar tokens a mano.
# Si falta cualquiera de las tres, la UI cae al modo manual sin romperse.
#
# META_APP_ID: developers.facebook.com → tu app → Configuración → Básica.
#   Es público (viaja al browser).
# META_ES_CONFIG_ID: en tu app → producto "Inicio de sesión de Facebook para
#   empresas" → Configuraciones → crear una configuración con la variación de
#   login "WhatsApp Embedded Signup" SELECCIONANDO productos (Cloud API): eso
#   la hace v4 automáticamente — desde el 15-oct-2026 las configs v2/v3 dejan
#   de funcionar. EVITA la plantilla "...With 60 Expiration Token": emite
#   tokens que expiran a los 60 días y Vocero necesita el business token sin
#   expiración. También es público.
# META_APP_SECRET (arriba): obligatorio para Embedded Signup — se usa SOLO en
#   el servidor para canjear el `code` por el token. Jamás sale al browser.
#
# Además, en el panel de Meta hay que cargar tu dominio en "Dominios permitidos
# para el SDK de JavaScript" y en "URI de redireccionamiento de OAuth válidos",
# o el popup falla en producción.
# META_APP_ID=
# META_ES_CONFIG_ID=

# OPCIONAL: Configuration ID separado para el flujo de coexistence ("Conectar
# sin perder el celular"). Normalmente NO hace falta: la misma configuración
# v4 sirve para ambos caminos (coexistence se selecciona en runtime con
# featureType). Llenala solo si comprobaste que tu config estándar no muestra
# la pantalla de conectar la app de WhatsApp Business en ese camino.
# META_ES_COEX_CONFIG_ID=

# --- Herramientas de demo (opcional) ------------------------
# Con DEMO_TOOLS_ENABLED=true aparece Ajustes → Datos, que permite RECARGAR el
# negocio de ejemplo aunque la organización ya tenga datos. A diferencia de los
# mocks, esto sí puede activarse en producción: sirve para preparar o mostrar
# una instancia. Es destructivo —reemplaza la base de conocimiento y las
# corridas del Laboratorio de la organización—, así que déjalo apagado una vez
# que el negocio esté operando de verdad.
# DEMO_TOOLS_ENABLED=true

# --- Inteligencia artificial (opcional) ---------------------
# El token del proveedor LLM y los modelos son POR EMPRESA: cada organización
# los configura en la app, en Ajustes → Inteligencia artificial (se guardan
# cifrados en la base). Sin config, esa empresa tiene el agente y el
# Laboratorio apagados con aviso; la bandeja manual funciona completa.
#
# DEPRECADAS (ya NO se leen en runtime): OPENROUTER_API_TOKEN,
# OPENROUTER_MODEL y OPENROUTER_JUDGE_MODEL. Si venías de una versión
# anterior con estas variables, pega el token (y los modelos, si quieres
# otros) en Ajustes → Inteligencia artificial de cada empresa y bórralas del
# entorno — dejarlas puestas no tiene ningún efecto.
#
# Lo ÚNICO que sigue siendo de instancia es la base del proveedor (misma API
# de OpenRouter para todas las empresas; el self-test la apunta al ai-mock):
# OPENROUTER_BASE_URL=https://openrouter.ai/api

# --- Administración de la plataforma (opcional) -------------
# Correos con acceso a la sección Administración (crear empresas y usuarios
# de esta instancia), separados por coma. El rol NO se guarda en la base:
# deriva de esta variable en cada request. Sin ella no hay super admin y la
# instancia opera como hasta ahora. Estos correos quedan RESERVADOS: nadie
# que no sea super admin puede crear cuentas con ellos (anti-escalación).
# SUPER_ADMIN_EMAILS=duena@tuagencia.com,respaldo@tuagencia.com

# --- Operación (opcional) -----------------------------------
# Reabrir el registro público después de la primera organización.
# ALLOW_SIGNUP=true

# Milisegundos de agrupación de mensajes antes del turno del agente (default 6000).
# AGENT_COALESCE_MS=6000

# --- Coexistence (opcional) ---------------------------------
# Muestra en Ajustes → WhatsApp la opción "Conectar sin perder el celular"
# (Embedded Signup con onboarding de la app de WhatsApp Business). Requiere
# estatus de Tech Provider aprobado por Meta (CorScan lo tiene desde el
# 3-sep-2026) y una Configuration v4 creada en el panel, con META_ES_CONFIG_ID
# apuntando a ella. Sin esos prerequisitos el popup falla al final.
# COEXISTENCE_UI_ENABLED=true
